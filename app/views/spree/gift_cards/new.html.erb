<h1><%= Spree.t(:gift_cards) %></h1>
<%= render "spree/shared/error_messages", :target => @gift_card %>
<%= form_for @gift_card do |f| %>
  <p>
    <%= f.label :variant_id, Spree.t(:value) %>
    <span class="required">*</span><br/>
    <%= f.select :variant_id, @gift_card_variants.map { |variant| [variant.display_price, variant.id] } %>
  </p>
  <p>
    <%= f.label :sender, "Sender's Name") %>
    <span class="required">*</span><br/>
    <%= f.text_field :sender %>
  </p>
  <p>
    <%= f.label :email, "Recipient's Email Address" %>
    <span class="required">*</span><br/>
    <%= f.text_field :email %>
  </p>
  <p>
    <%= f.label :name, "Recipient's Name" %>
    <span class="required">*</span><br/>
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.label :note, "Note" %><br/>
    <p class="help">Enter a note for the recipient. They'll receive this note with their gift card code.</p>
    <%= f.text_area :note, :rows => 10 %>
  </p>
  <%= f.submit Spree.t(:add_to_cart) %>
<% end %>
